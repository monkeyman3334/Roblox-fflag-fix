<Window x:Class="RobloxSettingsEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Roblox Fast Flag Editor" Height="600" Width="800"
        Background="#1E1E1E" Foreground="White"
        MinHeight="400" MinWidth="600">

    <Window.Resources>
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#505050"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="#707070"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0090FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252526" Padding="10">
            <StackPanel Orientation="Horizontal">

                <Button Content="📁 Select Settings File" 
                        Click="SelectFile_Click" 
                        Style="{StaticResource BaseButton}"/>

                <Button x:Name="ToggleReadOnlyButton"
                        Click="ToggleReadOnly_Click" 
                        IsEnabled="False"
                        Style="{StaticResource BaseButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="ToggleIcon" Text="🔒" FontFamily="Segoe UI Symbol" Margin="0,0,5,0"/>
                        <TextBlock x:Name="ToggleText" Text="Toggle Read-Only"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="FilePathLabel" 
                       Text="Current File: Not Selected" 
                       Foreground="#7AD7F0" FontSize="14" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="0"/>

            <TextBox x:Name="SettingsTextBox" Grid.Row="1"
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="#151515" Foreground="White" 
                     BorderBrush="#505050" BorderThickness="1"
                     FontFamily="Consolas" FontSize="12" Padding="5"/>

            <Button Grid.Row="2"
                    Content="💾 Save Changes" 
                    Click="SaveChanges_Click" 
                    Margin="0,15,0,0" 
                    Padding="15,8"
                    Style="{StaticResource AccentButton}"/>
        </Grid>

        <Border Grid.Row="2" Background="#000000" Padding="10">
            <TextBlock x:Name="StatusLabel" Text="Status: Ready." Foreground="#B0B0B0" FontSize="12"/>
        </Border>
    </Grid>
</Window>